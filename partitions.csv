# Carviston partition table — 16 MB flash (N16R8)
# Two OTA app slots (4 MB each) + NVS. The Vue web UI is embedded INSIDE the
# firmware image (see main/CMakeLists.txt), so there is no SPIFFS web partition
# — a firmware OTA always carries its matching UI. OTA slots are sized at 4 MB
# for the embedded UI (~0.35 MB) plus comfortable future growth.
# ~7.8 MB at the tail (0x830000..0x1000000) is intentionally unallocated —
# free to hand to bigger OTA slots later (that WOULD shift the NVS partitions,
# so it needs a fresh serial flash).
# Name,     Type, SubType,  Offset,    Size,    Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
ota_0,      app,  ota_0,    0x20000,   0x400000,
ota_1,      app,  ota_1,    0x420000,  0x400000,
storage,    data, nvs,      0x820000,  0x10000,
